
.all-index
  .Header
    %p.Image
      =image_tag "foundmuji.png", alt: "Found MUJI", class: "image"

  %p.first-message　無印良品スタッフ専用口コミアプリ。おすすめをつぶやこう。

  .Search__Btn
    = form_with(url: search_items_path, local: true, method: :get, class: "search-name") do |form|
      = form.text_field :keyword, placeholder: "商品を検索する", class: "search-input"
      = form.submit "さがす", class: "search-btn" 

  -#.Search__Btn
    -#= form_with model: @item do |form|
      -#=form.text_field :items, {placeholder:"ITEM",class:"search-name"}
      -#=form.submit 'SEARCH',class: "search-btn"
  .Tbtn    
    .Create__Btn
      = link_to "つくる", new_item_path, method: :get, class: 'create-btn'

    .Like__Btn
      = form_with model: @item do |form|
        = form.submit "いいね", class: 'like-btn'

    .Logout__Btn
      = link_to "ログアウト", destroy_shop_session_path, method: :delete, class: 'logout-btn'

